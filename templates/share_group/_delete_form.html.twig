<form method="post" action="{{ path('share_group_delete', {'id': share_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ share_group.id) }}">
    <button class="btn">Delete</button>
</form>
